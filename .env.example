# ============================================
# Microsoft Graph MCP Server - 環境変数設定
# ============================================
#
# 設定するのは 4 つだけ。TOKEN_ENCRYPTION_KEY は不要です
# （MICROSOFT_CLIENT_SECRET + TENANT_ID から自動導出されます）。

# ── 管理パスワード (必須) ──
# /auth ページのアクセス保護用。自分で決めた任意の文字列。
ADMIN_SECRET=your-admin-password-here

# ── Azure AD アプリ設定 (必須) ──
# Azure Portal > Microsoft Entra ID > アプリの登録 で作成
MICROSOFT_CLIENT_ID=your-application-client-id
MICROSOFT_CLIENT_SECRET=your-client-secret
MICROSOFT_TENANT_ID=your-directory-tenant-id

# ── 以下は自動設定 ──
# Vercel Marketplace の Upstash Redis 統合で自動設定:
# KV_REST_API_URL=https://xxx.upstash.io
# KV_REST_API_TOKEN=xxx
#
# Vercel が自動設定:
# CRON_SECRET=xxx
#
# オプション（通常不要）:
# TOKEN_ENCRYPTION_KEY を明示指定したい場合のみ設定。
# 未設定の場合は CLIENT_SECRET + TENANT_ID から HKDF で自動導出されます。
# TOKEN_ENCRYPTION_KEY=your-hex-string
